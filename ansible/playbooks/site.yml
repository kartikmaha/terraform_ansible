---
# PLAY 1: Connection Check
- name: Wait for EC2 SSH to be ready
  hosts: web_servers
  gather_facts: false
  tasks:
    - name: Ensure port 22 is reachable
      ansible.builtin.wait_for:
        port: 22
        host: "{{ inventory_hostname }}"
        search_regex: OpenSSH
        delay: 15
        timeout: 300
      delegate_to: localhost

# PLAY 2: Infrastructure Configuration
- name: Configure Web Server and Deploy App
  hosts: web_servers
  become: true
  gather_facts: false 
  
  vars:
    ansible_python_interpreter: /usr/bin/python3
    app_port: 80 
    # This must match what was pushed in the deploy.yml
    docker_image: "kartikmaha/terraform_ansible:latest"

  tasks:
    - name: Install Docker
      ansible.builtin.dnf:
        name: docker
        state: present
        update_cache: true

    - name: Start and enable Docker service
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: true

    - name: Add ec2-user to docker group
      ansible.builtin.user:
        name: ec2-user
        groups: docker
        append: true

    - name: Reset connection to apply group changes
      ansible.builtin.meta: reset_connection

    - name: Pull and run App
      community.docker.docker_container:
        name: flask_app
        image: "{{ docker_image }}"
        state: started
        pull: true
        restart_policy: always
        published_ports:
          - "{{ app_port }}:3000"
