---
- name: Deploy Application using Ansible
  hosts: web
  become: true

  vars:
    app_dir: /home/ec2-user/app
    docker_image: myapp
    container_name: myapp
    app_port: 80

  roles:
    - role: app-role
